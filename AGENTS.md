# AGENTS.md

## 项目定位

本项目用于构建一个**多井通用的固井顶替效率模型**。目标不是继续堆积单井脚本，而是逐步沉淀成可复用的 Python 包：后续通过更换井资料、流体程序和施工时序，即可复算不同井的实时顶替效率和最终水泥浆顶替效率。


---

## 当前推荐架构

模型按分层结构演进：

1. **地面施工程序层**：表达现场分段注入、排量切换、停泵/续泵；
2. **套管内 1D 输运层**：从地面开泵开始追踪流体在套管内下行，必要时考虑套管内混合；
3. **鞋口边界桥接层**：把鞋口处出流状态转成环空入口边界；
4. **环空 2D 顶替核心层**：计算偏心环空中的顶替、窜槽、混浆、失稳和泥饼清除；
5. **诊断与质量解释层**：输出实时效率、最终效率、风险指标和质量代理值。

在真正实现多井复用之前，优先做：

- 标准输入数据结构；
- 通用 `cemdisp` 包骨架；
- Hu102 / Hu101 逻辑的可迁移接口；
- 不破坏现有 legacy 脚本可运行性。

---

## 文件与目录约定

### Python 模块与包

- 包名、模块名、类名、函数名使用**英文**；
- 目录结构优先放在 `cemdisp/` 下；
- 新增逻辑优先进入：
  - `cemdisp/data/`
  - `cemdisp/transport1d/`
  - `cemdisp/models2d/`
  - `cemdisp/diagnostics/`
  - `cemdisp/reporting/`
  - `cemdisp/validation/`

### 用户可见输出

用户可见输出优先使用**中文**，包括但不限于：

- 图表标题；
- 坐标轴标题；
- 图例文字；
- 报告标题；
- 结果摘要字段名；
- 生成文件名。

### 文件命名规则

输出文件名尽量使用**中文描述在前**的形式，例如：

- `呼102尾管_时间序列结果.csv`
- `呼101尾管_深度效率剖面对比.png`
- `多井固井顶替效率模型结果摘要.md`

模块文件名仍保持英文，例如：

- `cemdisp/models2d/annulus_d2dga.py`
- `cemdisp/transport1d/casing_flow.py`

---

## 图表输出要求（必须遵守）

### 1. 中文图例和中文标签

凡是项目自动生成的图表：

- 图例必须为中文；
- 坐标轴文字必须为中文；
- 标题必须为中文；
- 若同时存在“技术内部字段名”和“用户显示名”，显示名必须优先中文。

### 2. 中文文件名优先

图表导出文件名、摘要文件名、CSV/JSON/Markdown 输出文件名，应尽量使用中文描述。

### 3. 字体兼容性

绘图代码必须显式考虑中文字体兼容。新增绘图模块时，至少要保证：

- 常见 Windows 中文字体优先；
- 负号显示正常；
- 在中文字体缺失时给出可接受回退策略。

### 4. 口径清晰

图表中严禁把以下概念混写：

- 顶替效率；
- 有效顶替效率；
- 质量响应效率；
- CBL 合格率代理值。

显示时必须明确中文名称，避免误导。

---

## 数据与求解边界约束

### 1. 不要在通用模块中硬编码单井数据

以下内容不得直接写死在通用求解器里：

- 井号；
- 固定井深；
- 单井评价井段；
- 固定排量；
- 固定流体体积；
- 单井专用文件路径。

这些应进入：

- `WellSpec`
- `FluidSpec`
- `PumpingSchedule`
- per-well loader



`quality_proxy` 不是顶替效率真值，不能与 `effective_displacement_efficiency` 混为一谈。

### 3. 套管内建模是可插拔层

套管内 1D 输运层的目标是提供更真实的鞋口出流边界，而不是污染环空 2D 核心。设计时应保持：

- 环空求解器可独立运行；
- 1D → 2D 边界桥接接口清晰；
- 后续可选加入套管内对流-弥散，而不重写环空核心。

---

## 对 legacy 脚本的处理规则

以下目录中的脚本视为 legacy 参考资产：

- `hu101model/`
- `hu102model/`

默认规则：

1. 不随意大改 legacy 文件；
2. 若需复用逻辑，优先迁移到 `cemdisp/` 新模块；
3. 若必须改 legacy 文件，改动必须与当前任务直接相关；
4. 不要把新功能继续堆在 legacy 脚本顶部。

---

## 开发阶段建议

当前阶段优先级：

1. 建立 `cemdisp` 可安装骨架；
2. 建立标准数据结构；
3. 建立套管内 1D 层与环空 2D 层的目录占位；
4. 后续再迁移 Hu102 / Hu101 求解逻辑；
5. 最后统一报告与图表输出。

当前阶段**不要**：

- 一次性追求完整 3D CFD；
- 做过重的抽象基类体系；
- 在没有数据结构之前先写复杂求解器；
- 为了“通用”而引入大量未被当前场景使用的配置项。

---

## 验证要求

每次新增骨架或模块后，至少完成以下检查：

1. 新模块可被 Python 导入；
2. 包安装路径不再与 `pyproject.toml` 声明冲突；
3. 类型与基本结构没有明显语法错误；
4. 若新增输出模块，中文标题、图例、文件名要求得到满足。

在实现求解逻辑后，再逐步增加：

- 质量守恒验证；
- 时间推进稳定性验证；
- 井段对比验证；
- 多井切换验证。

---

## 代码内容要求

本项目所写的代码同时要附上中文的注释，确保后续维护个修改时能读懂每块代码的含义，所实现的功能以及各个变量名字所指代的含义。

## 文档优先级

本项目的结构、建模和开发路线，优先参考以下文档：

1. `docs/multiwell_primary_cementing_model_scheme.md`
2. `docs/multiwell_primary_cementing_development_task_list.md`
3. `README.md`

新增实现如果与上述文档不一致，应优先更新文档或在实现说明中解释偏差原因。

