# cement model 代码冗余审计报告

> 生成日期：2026-07-16
> 方法：4 路并行子代理（核心包死代码 / runner+scripts / 资产与文档 / Obsidian 笔记挖掘）+ 对高风险删除项的亲自 Grep 复核。
> 覆盖范围：`cemdisp/` 核心包 + `scripts/` + `tests/` + 顶层资产 + `docs/`。不含 `参考文档/` 现场资料库本身。
> 基线文档：`docs/模型求解流程与代码结构详解报告.md`（2026-07-14，前天）。

---

## 0. 先纠正基线报告/笔记里的 3 处偏差

| 来源说法 | 实测结论 | 证据 |
|---|---|---|
| Obsidian 笔记6 + 求解流程报告称 `_buoyancy_force_vector` 是死代码 | **非死代码**。被 `annulus_d2dga.py:768` 调用，返回 `f_phi/f_xi` 喂给 R2 的 `d2dga_buoyancy_flux` 方向因子。仅 R3 体力注入路径（`:599-603`）是占位 | Grep + Read |
| 求解流程报告把 `diagnostics/`、`validation/` 当作第5层在用 | **生产链路零调用**。`compute_cbl_quality_proxy`/`validate_against_cbl`/`validate_cement_mass_balance` 仅自身 `__init__` 再导出，无 runner/script import | Grep 全仓库 |
| 子代理曾忽略 `hu102model/` | **必须保留**：`hu102_loader.py:56` `DEFAULT_CALIPER_CSV = .../hu102model/hu102_tail_caliper_inclination.csv` | Read hu102_loader.py |

---

## 1. 分层删除建议总览

按"删除安全性"分 6 层。**Tier 1-2 可立即执行**；**Tier 3 需你拍板**（删的是有意保留的架构层）；**Tier 4 是重构非删除**；**Tier 5 是改进目标非删除**；**Tier 6 是资产归档**。

---

## Tier 1 — 可直接删（零生产依赖、临时文件、构建产物，无行为变化）

| 路径 | 性质 | 证据 | 操作 |
|---|---|---|---|
| `cankao/__temp_*.txt`（4 个） | PDF 抽取临时文本 | 0 引用，git 未跟踪 | 删 |
| `temp_pdf_images/`（13 PNG，~79MB） | PDF 抽图临时目录 | 0 引用，但被 git 跟踪 | `git rm -r --cached` + 删 |
| `_paper_text.txt`（108KB，根目录） | JFM 947 A32 论文全文文本 dump | 0 引用（非项目草稿） | 删 |
| `docs/code_file_guide.md` | 描述已不存在的旧筛选模型文件 | git 已标 `D`，内容误导 | 提交删除 |
| 已跟踪的 `__pycache__/*.pyc` | 字节码构建产物 | `.gitignore` 已排除但仍跟踪 | `git rm -r --cached` |
| `cemdisp.egg-info/` | 构建产物 | `.gitignore` 已列 `*.egg-info/` | `git rm -r --cached` |
| `.pytest_cache/` | pytest 缓存 | 0 引用，`.gitignore` 未列 | 删 + 加 `.gitignore` |
| `cemdisp/correlations/__init__.py` | 空占位包 | 全仓库 0 import | 删整个子包 |
| `cemdisp/utils/__init__.py` | 空占位包 | 全仓库 0 import | 删整个子包 |
| `scripts/run_{hu101,hu102,hu103,hu1,hu2,ht1_001}_tailpipe_initial.py`（6 个） | 1-2 行薄 wrapper | runner 自身已有 `__main__`；`test_all_wells.py` 直接 import runner | 删 |
| `scripts/generate_test_report.py` | 孤儿，硬编码过时 6 井状态 | 0 引用，已被 `test_all_wells.py` 取代 | 删 |
| `scripts/generate_fig2_eccentric_annulus.py` | 功能被覆盖 | 0 引用，fig02 已由 `plot_patent_figures.py` 生成 | 删 |

**预计释放：~80MB+（主要是 temp_pdf_images）+ 清理 git 跟踪。**

---

## Tier 2 — 死代码，删需连带调整测试（低风险，机械）

| 文件:行 | 对象 | 证据 | 连带 |
|---|---|---|---|
| `cemdisp/runners/common.py` | `export_old_vs_new_inlet_comparison`（整文件唯一函数） | 仅 `tests/test_runner_common.py:12` 调用 | 删文件 + 删测试 |
| `cemdisp/data/provenance.py:194` | `build_multiwell_provenance_summary` | 仅 `tests/test_provenance.py:46,67` | 删函数 + 删测试用例 |
| `cemdisp/data/loaders/__init__.py:74-97` | `REFERENCE_DOCS_ROOT`、`available_well_names()`、6 个 `*InletProviderFactory` | 0 外部使用 | 删导出 |
| `cemdisp/data/loaders/base.py` | 整文件（9 个 public helper） | 0 import；各 loader 私有复制了同名 helper | 删文件 |
| 6 个 loader 的 `build_*_annulus_inlet_provider`（如 `hu101_loader.py:213`） | 已 `DeprecationWarning` | 仅 `tests/test_loader_deprecation.py` 触发 | 删函数 + 删弃用测试 |
| `cemdisp/models2d/annulus_d2dga.py:345,417` | `gel_strength`、`temperature_correction` 参数 | 进函数即 `del`，从未使用 | 删参数（保留 `None` 占位亦可） |
| `cemdisp/models2d/annulus_d2dga.py:140` | `AnnulusSimulationResult.reynolds_snapshots` | 注释"Legacy field -- always empty"，0 写入/读取 | 删字段（旧结果反序列化会破，可接受） |
| `cemdisp/reporting/contour_plots.py:270` | "壁面泥饼" panel 分支 | `wall_field` 恒零，`np.max(|wall|)>1e-12` 永假 | 删分支 |
| `cemdisp/transport1d/interface_tracking.py` | `InterfaceTracker` 类 | 0 外部调用（`__init__` 注释"保留用于可视化/非恒定排量"） | 删或保留占位（见 Tier5 说明） |

> 注：`base.py` 删除后，若后续做 Tier 4 loader 去重，应**重建** base.py 作为公共层（不是现在这个无人用的版本）。

---

## Tier 3 — 未接线的架构层，需你决策（删 = 移除有意保留的第5层）

这些模块**当前生产链路完全不调用**，但 AGENTS.md / README / 求解流程报告都把它们描述为"第5层诊断/验证"。删之前请明确：是**接进去用**，还是**确认放弃后删**？

| 模块 | 现状 | 选项 |
|---|---|---|
| `cemdisp/validation/mass_balance.py` | `validate_cement_mass_balance` 0 调用 | (a) runner 里接质量守恒校验 (b) 删 |
| `cemdisp/validation/cbl_comparison.py` | `validate_against_cbl`/`summarize_validation` 0 调用 | (a) runner 里接 CBL 对比 (b) 删 |
| `cemdisp/diagnostics/quality_proxy.py` | `compute_cbl_quality_proxy`/`compute_from_simulation_metrics` 0 调用；`quality_proxy_pct` 列仅在旧 CSV 里 | (a) 接进诊断输出 (b) 删 |
| `cemdisp/reporting/ablation_plots.py` | `plot_fig6/7/8/9`+`generate_all_ablation_figures` 0 import | ⚠️ 文档称其生成论文 fig6-9。删前确认论文图是否已由 `scripts/paper_data/` 另生成 |
| `wall` 场（`annulus_d2dga.py:680` 恒零 + 结果 schema + contour_plots panel） | 占位，泥饼清除未实现 | 涉及 `AnnulusSimulationResult` 与多个消费者，删改面较大 |

**我的建议**：`validation/` 和 `diagnostics/` **不要急着删**。它们是 AGENTS.md 钦定的"只验证不校准"层，是论文闭环的一部分。先确认是"忘了接"还是"决定不用"——前者应补接线（接进 runner 的步骤6/7），后者再删。`ablation_plots.py` 同理，先确认论文 fig6-9 的真实生成路径。

---

## Tier 4 — 高价值重构（去重，非删除）

1. **8 个 per-well runner 抽公共基类**（最高价值）。重复块：import 块、`PROJECT_ROOT`、`run_and_export` 主流程、`annulus_stop_time_s`（8 份，且都 `del fluids` 死参数）、`_export_casing_flow_timing`、`_export_table_csv`、`_is_cement_slurry_name`、`_annulus_phase_label`。每井独有仅：loader、中文井名、停时策略（2 类）、`nz`、`split_cement_phases`、NPZ 字段、时序表水泥识别、是否调 `export_reference_figure_set`、是否加 provenance。→ `common.py` 变成真正的 `WellRunner` 基类 + 每井配置，runner 退化到 30-50 行。
2. **loader 公共 helper 下沉**：各 loader 复制了 `_equivalent_hole_diameter_mm`/`_build_hole_profile`/`_surface_state`/入口 provider。→ 重建 `base.py` 作公共层（删旧版后重写）。
3. **`ht1_003_sensitivity.py` 与 `ht1_004_sensitivity.py`** 结构几乎全同，仅 loader/基线/停时口径不同 → 抽公共扫描+绘图。

---

## Tier 5 — 改进目标（能跑但是代理/遗留实现，你笔记里想"替换"而非"删除"）

来自 Obsidian 笔记 1/2/3/5/6 的研究结论。这些**不是死代码**，是"半套 D2DGA + 经验代理"，删了会丢功能。列为你的改进路线图：

| 对象 | 现状 | 改进方向 | 来源 |
|---|---|---|---|
| `buoyancy_shape` 浮力代理 | 线性方位角再分配 + 经验裁剪 | 改为论文浮力体力 | 笔记1 |
| 横向速度 `v`（连续性反推） | 运动学代理 | 改椭圆流函数 Ψ | 笔记1/6 |
| `effective_efficiency` | 等于 `bulk_cement_fill`，无"有效"修正 | 补有效修正 | 笔记1/3/6 |
| 默认 `m=1.0` 全局常数 | — | 局部自动 `μ_displaced/μ_displacing` | 笔记1 |
| `rho_disp=0.67·ρ_lead+0.33·ρ_tail` | 硬编码权重 | 局部浓度加权密度 | 笔记1 |
| `open_outlet=True` 不限体积 | — | 收紧到守恒口径 | 笔记1 |
| `_limit_phase_volume` | 事后补丁非守恒解 | 守恒形式 + H 加权 | 笔记1 |
| `instability_proxy` | 纯诊断不反馈浓度 | 视需要反馈或仅留诊断 | 笔记1 |
| 套管 Bingham/HB `α·U·R` 弥散 | ad-hoc 退化 | JNNFM 2016 屈服流体 Taylor 弥散 | 笔记1/2 |
| `D_mol=1e-9` 硬编码 | — | 参数化 | 笔记1 |
| `boundary_bridge` 旧方式接口 | 基于 `CasingFlowResult`/`CasingFlowSolver` 的兼容路径 | 确认无人用后删 | 笔记3 |
| 偏心度 `e=clip(...,0.05,0.55)` | standoff≤0.40 后饱和 | 复核上限 | 笔记6 |
| `dt=4.0` 固定 | 无 CFL 自适应 | 主循环动态 dt | 笔记6 |

> ⚠️ `_buoyancy_force_vector` **不在删除/替换清单**——你的笔记6说它是死代码，但实测它喂给了 R2 的 I3 通量方向因子（见 §0 纠正）。R3 的"真浮力体力注入"才是占位，别混淆。

---

## Tier 6 — 资产归档（非代码）

| 路径 | 处理 |
|---|---|
| `参考文档/hu101_1d2d_paper_project/`（旧 1D2D 快照） | 被 3 个 archive 候选脚本读其 CSV（`plot_hu101_full_depth_distribution.py`、`_ref.py`、`_ref_v2.py`）。**归档这 3 个脚本后，整个快照可删**，或先把 CSV 迁到 `参考文档/呼101/` 再删 |
| `.superpowers/sdd/`（task-1~7，已 DONE） | 过程产物，可归档 |
| `docs/superpowers/plans/` 5-6 月计划 | 已完成，归档；保留 7 月当前计划 |
| `docs/superpowers/specs/` | 被 `run_paper_main_results.py` 等引用，保留 |
| `docs/multiwell_primary_cementing_development_task_list.md` | 部分过期（引用已不存在的 `geometry.py`/`fluid.py`/`dispersion.py`），更新或标注过期 |
| `baogao/模型影响因素与数据层分析报告.md` | 未跟踪草稿，与 `docs/模型求解流程与代码结构详解报告.md` 重叠，合并或删 |
| `scripts/plot_hu101_cbl_quality_vs_efficiency_ref.py`、`_ref_v2.py`、`plot_hu101_full_depth_distribution.py`、`compare_old_new_model.py`、`closure_contribution_scan.py`、根 `plot_segment_quality.py` | 一次性/版本变体，结果已落盘，归档 |
| `论文构思及草稿/` | 工作草稿，保留 |

---

## 2. 建议执行顺序

1. **先做 Tier 1**（纯收益，~80MB+，零风险）。
2. **再做 Tier 2**（连带删测试，跑 `pytest` 确认全绿；环境 `conda activate shenjingwangluo`）。
3. **Tier 3 先别动**，逐个确认"接线 vs 删除"后再处理。`ablation_plots.py` 尤其要先查论文 fig6-9 来源。
4. **Tier 4 重构**作为独立 PR，配合 Tier 2 的 `common.py` 重建一起做。
5. **Tier 5/6** 按研究进度推进。

## 3. 验证方法（删后回归）

```powershell
conda activate shenjingwangluo
python -m pip install -e .
python -m pytest tests/ -v          # 重点: test_d2dga_flux / test_improved_d2dga_annulus / test_six_well_integration
python scripts/test_all_wells.py   # 8 井端到端
```

核心算法动之前先跑：`python -m pytest tests/test_d2dga_flux.py tests/test_improved_d2dga_annulus.py -v`。
