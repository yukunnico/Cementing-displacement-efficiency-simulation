# 代码死代码审计报告（2026-07-29）

> 生成日期：2026-07-29
> 方法：全仓库 grep 调用链追踪，覆盖 cemdisp/ 52 个 Python 文件
> 前序报告：`代码冗余审计_20260716.md`（2026-07-16）
> 本次目标：验证旧审计结论是否仍成立 + 发现新死代码

---

## 0. 与旧审计对比：已清理项 ✅

| 旧审计项 | 旧状态 | 当前状态 |
|----------|--------|---------|
| `cemdisp/correlations/` 空占位包 | 待删 | ✅ 已删 |
| `cemdisp/utils/` 空占位包 | 待删 | ✅ 已删 |
| `cemdisp/data/loaders/base.py` | 待删 | ✅ 已删 |
| `cemdisp/runners/common.py` | 待删 | ✅ 已删 |
| `build_*_annulus_inlet_provider` 弃用函数 | 待删 | ✅ 已删 |
| `gel_strength`/`temperature_correction` 参数 | 进函数即 del | ✅ 已删 |

---

## 1. 完全死掉的模块（零外部导入）

| # | 模块 | 证据 | 建议 |
|---|------|------|------|
| 1 | `cemdisp/reporting/ablation_plots.py` | 全仓库无任何 `from cemdisp.reporting.ablation_plots import` | 删整文件 |
| 2 | `cemdisp/validation/cbl_comparison.py` | `validate_against_cbl`/`summarize_validation` 仅 __init__.py 导出，0 调用 | 删或接线 |
| 3 | `cemdisp/validation/mass_balance.py` | `validate_cement_mass_balance` 仅 __init__.py 导出，0 调用 | 删或接线 |

---

## 2. 死函数/死类（在活跃模块内）

| # | 对象 | 文件:行 | 证据 | 建议 |
|---|------|---------|------|------|
| 4 | `InterfaceTracker` 类 | `transport1d/interface_tracking.py` | 0 外部调用（仅 `InterfaceFront` 被 `casing_flow.py:51` 用） | 删类，保留 `InterfaceFront` |
| 5 | `compute_cbl_quality_proxy` | `diagnostics/quality_proxy.py` | 0 生产调用（仅 __init__.py 导出） | 删或接线到 runner |
| 6 | `build_multiwell_provenance_summary` | `data/provenance.py:194` | 0 调用 | 删函数 |
| 7 | `AnnulusSimulationResult.reynolds_snapshots` | `models2d/annulus_d2dga.py:147` | 恒空 tuple，注释"Legacy field"，0 写入/读取 | 删字段 |
| 8 | `pipe_exit_to_annulus_inlet` | `models2d/boundary_bridge.py:59` | 0 外部调用（仅 __init__.py 导出） | 删函数 + 清 __init__ 导出 |
| 9 | `available_well_names`/`REFERENCE_DOCS_ROOT`/`InletProviderFactory` | `data/loaders/__init__.py` | 0 外部调用 | 删导出 |

---

## 3. 各层调用关系图

```
runners/ (8井 + 1消融)
  ├─ data/loaders -> WellSpec, FluidSpec, PumpingSchedule, ValidationData
  ├─ transport1d -> CasingFlowSolver, CasingFlowResult
  ├─ models2d -> AnnulusD2DGASolver, build_coupled_annulus_inlet_provider
  ├─ reporting -> animate, contour_plots, plots, reference_figures
  └─ data/fluid_provenance -> build_injected_fluid_provenance_summary

data/ 层
  ├─ fluid_spec.py     ✅ 活跃
  ├─ well_spec.py      ✅ 活跃
  ├─ pumping_schedule.py ✅ 活跃
  ├─ validation_data.py ✅ 活跃
  ├─ provenance.py     ✅ 活跃（build_multiwell_provenance_summary ❌ 死）
  ├─ fluid_provenance.py ✅ 活跃
  └─ loaders/ (8个) ✅ 活跃（__init__.py 有 3 个死导出）

transport1d/ 层
  ├─ casing_flow.py    ✅ 活跃（pipe_exit_state_at 旧接口仍被 7 runner + bridge 调用）
  ├─ shoe_timeline.py  ✅ 活跃
  ├─ pipe_exit_state.py ✅ 活跃
  └─ interface_tracking.py ⚠️ 部分死（InterfaceTracker ❌，InterfaceFront ✅）

models2d/ 层
  ├─ annulus_d2dga.py  ✅ 活跃（reynolds_snapshots 字段 ❌ 死）
  ├─ d2dga_flux.py     ✅ 活跃
  └─ boundary_bridge.py ✅ 活跃（pipe_exit_to_annulus_inlet ❌ 死）

diagnostics/ 层
  ├─ tier0_diagnostics.py ✅ 被 annulus_d2dga.py:1129 调用
  ├─ displacement_metrics.py ✅ 被 tier0 调用
  ├─ flow_classification.py ✅ 被 tier0 调用
  ├─ muskat_regime.py ✅ 被 tier0 调用
  ├─ regime_classifiers.py ✅ 被 tier0 调用
  └─ quality_proxy.py ❌ 0 生产调用

validation/ 层 ❌ 整层未接线
  ├─ cbl_comparison.py -> 仅 __init__.py
  └─ mass_balance.py -> 仅 __init__.py

reporting/ 层
  ├─ plots.py           ✅ 活跃
  ├─ contour_plots.py   ✅ 活跃
  ├─ animation.py       ✅ 活跃
  ├─ reference_figures.py ✅ 活跃
  └─ ablation_plots.py  ❌ 0 导入
```

---

## 4. 建议执行顺序

### 第一步：零风险删除（不影响任何测试）
1. 删 `reporting/ablation_plots.py` 整文件
2. 删 `InterfaceTracker` 类（保留 `InterfaceFront`）
3. 删 `AnnulusSimulationResult.reynolds_snapshots` 字段
4. 删 `pipe_exit_to_annulus_inlet` 函数 + 清理 `__init__.py` 导出
5. 删 `build_multiwell_provenance_summary` 函数
6. 删 `loaders/__init__.py` 中的 `available_well_names`/`REFERENCE_DOCS_ROOT`/`InletProviderFactory`

### 第二步：需决策（删 or 接线）
7. `validation/` 整包：接线到 runner（质量守恒校验 + CBL 对比）or 删
8. `diagnostics/quality_proxy.py`：接线到诊断输出 or 删

### 第三步：验证
```powershell
conda activate shenjingwangluo
python -m pytest tests/ -v
python scripts/test_all_wells.py
```
