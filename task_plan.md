# 0708资料梳理与多井数据接入规划

## 目标

- 以 `D:\users\desktop\research\控压固井项目\0708` 为当前首要资料来源，系统梳理可支撑 `cemdisp` 套管内 1D 与环空 2D 模型的数据。
- 暂不处理 `cement model\参考文档` 目录中其他井的既有资料差异，优先围绕 0708 资料建立“所需资料清单—缺口—归档计划—后续接入顺序”。
- 为后续把现场施工流程、流体信息、井身结构、工艺参数等补入 `cement model\参考文档` 制定明确执行计划。

## 当前范围

- 重点目录：`0708/`、`cement model/cemdisp/`、`cement model/scripts/`、`cement model/参考文档/`
- 当前产出：资料摸底结果、模型输入资料清单、缺失项分析、阶段性执行计划
- 暂不处理：`cement model/参考文档/` 中其他井的逐井核对与修订

## 阶段计划

| 阶段 | 状态 | 说明 |
|---|---|---|
| 阶段1 | complete | 恢复/建立规划文件与任务边界 |
| 阶段2 | complete | 摸底 0708 资料目录结构、文件类型、可提取信息 |
| 阶段3 | complete | 摸底 cemdisp、scripts、参考文档 的当前数据接入与归档方式 |
| 阶段4 | in_progress | 建立模型所需资料清单与缺失矩阵 |
| 阶段5 | pending | 输出阶段性执行计划与优先级建议 |

## 当前决策

- 以 0708 资料为本轮优先输入源。
- 计划先完成“资料治理与接入规划”，再决定具体补录与代码接入节奏。
- 外部资料内容只记录到 `findings.md`，避免把不可信原文写入本文件。

## 风险与注意事项

- 0708 中可能包含 PDF、Word、Excel、图片等多种格式，需分别采用合适的抽取方式。
- 模型输入字段必须避免重新落回“单井脚本硬编码”，应优先映射到统一数据结构。
- `quality_proxy` 等质量代理指标不能与真实顶替效率字段混写。

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| `multimodal-looker` 无法访问工作区外 PDF 路径 | 1 | 改走主代理直接 `read` PDF / 读取配套 txt 文本 |
| `soffice not found`，无法将旧 Word 渲染为 PDF | 1 | 改走 `pandoc --extract-media` 处理 `docx`；旧 `.doc` 后续优先转存或由用户提供可访问副本 |
