**/*.xlsx
# results/ 下口径说明类 sidecar 一律用 .md/.json，**/*.txt 会吞掉它们（如 口径说明.md）
**/*.txt
**/*.doc
**/*.docx
**/*.pdf
**/*.tmp
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
# 例外：scripts/lib/ 是本仓库的脚本共享模块目录，必须入库
# （上一条 `lib/` 无前导斜杠，会匹配任意层级的 lib 目录，静默吞掉 scripts/lib/ 下的新模块）
!scripts/lib/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/

# PyCharm / VSCode
.idea/
.vscode/
*.swp
*.swo
*~
# CBL 声幅图切片中间产物（PNG 大图，可由 PDF 重生成，不入库）
参考文档/现场资料提取/**/cbl_pages/*.png
参考文档/现场资料提取/**/cbl_digitization/pages/*.png
参考文档/现场资料提取/**/cbl_pages/test_slice_top.png
# 临时对比脚本与输出（scratch，不入库）
_tmp_*.py
_t10t11_out/
.tmp_research/

# 大体积二进制产物：不入库、不推送（源码与文本口径说明除外）
# 注：旧规则 `2D场数据.npz` 无通配符，只匹配同名文件，对
# `呼102尾管_1D2D耦合模型_2D场数据.npz` 这类实际文件名**无效**——已改为 `*.npz`
*.npz
*.zip
*.gif
